from typing import List

# Write function headers and docstring for the following:

# sum
def sum(numbers: List[float]) -> float:
    """Adds up all numbers in a list.

    Args:
        numbers: A list a numbers.
    
    Returns:
        The sum of the numbers in the list.
    """
    pass

# math.ceil
def ceil(num: float) -> int:
    """Rounds a decimal number to the nearest greater integer.

    Args:
        num: A decimal number.
    Returns:
        The number rounded up.    
    """
    pass

# math.floor
def floor(num: float) -> int:
    """Rounds a decimal number down to the nearest integer.

    Args:
        num: Decimal number to round down.
    Returns:
        The number rounded down.
    """
    pass

# Function that takes a name and returns a message
def create_message(name: str) -> str:
    """Returns a personalized message

    Args:
        name: Someone's name
    Returns:
        A personalized message including someone's name.
    """
    pass

# Function that takes two floats and finds their average.
def avg(a: float, b: float) -> float:
    """Gets the average of two numbers.

    Args:
        a: A number
        b: Another number
    Returns:
        The average of the two numbers.
    """
    pass

# Function that takes an age and returns True if they are of voting age
def can_vote(age: int) -> bool:
    """Determines whether or not someone can vote.

    Args:
        age: The age of some person.
    Returns:
        True if they can vote, False otherwise.
    """
    pass
